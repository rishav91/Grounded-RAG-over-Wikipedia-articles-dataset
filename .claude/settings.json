{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.py) .venv/bin/ruff check --fix --quiet \"$f\"; .venv/bin/ruff format --quiet \"$f\" ;; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
